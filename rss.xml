---
layout: null
permalink: /rss.xml
---

<?xml version="1.0" encoding="UTF-8"?>

<rss version="2.0"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:atom="http://www.w3.org/2005/Atom">

<channel>

<title>{{ site.title | xml_escape }}</title>
<link>{{ site.url }}</link>
<description>{{ site.description | strip_html | xml_escape }}</description>

<atom:link href="{{ site.url }}/rss.xml"
           rel="self"
           type="application/rss+xml"/>

<lastBuildDate>{{ site.time | date_to_rfc822 }}</lastBuildDate>

{% for post in site.posts limit:15 %}

<item>

<title>{{ post.title | xml_escape }}</title>

<link>{{ site.url }}{{ post.url }}</link>

<guid isPermaLink="true">
{{ site.url }}{{ post.url }}
</guid>

<pubDate>{{ post.date | date_to_rfc822 }}</pubDate>

<description>
<![CDATA[
{{ post.excerpt | strip_html | truncate: 280 }}
]]>
</description>

{% capture email_content %}
{{ post.content
   | replace: '<div class="language-', '<pre><code>'
   | replace: '<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>', '<pre><code>'
   | replace: '</code></pre></div></div>', '</code></pre>'
   | replace: '</div>', ''
}}
{% endcapture %}

<content:encoded>
<![CDATA[
{{ email_content }}
]]>
</content:encoded>

</item>

{% endfor %}

</channel>
</rss>
